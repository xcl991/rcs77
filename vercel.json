{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "devCommand": "next dev",
  "functions": {
    "app/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
